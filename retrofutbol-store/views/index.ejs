<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Retro Futbol Store</title>

  <!-- Bootstrap + Icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">

  <link rel="stylesheet" href="/stylesheets/style.css" />
  <style>
    /* ---- Hero moderno ---- */
    .hero {
      position: relative;
      min-height: 68vh;
      display: grid;
      place-items: center;
      color: #fff;
      text-align: center;
      overflow: hidden;
      background: #0b1b2b;
      background-image:
        linear-gradient(180deg, rgba(0,0,0,0.35), rgba(0,0,0,0.65)),
        url('/images/hero.jpg');
      background-size: cover;
      background-position: center;
    }
    .hero .hero-inner { z-index: 2; max-width: 900px; }
    .hero h1 { font-weight: 800; letter-spacing: .5px; }
    .hero p.lead { opacity: .95; }

    /* ---- Secciones ---- */
    .section-title {
      font-weight: 700;
      letter-spacing: .3px;
    }

    /* ---- Tarjetas de producto ---- */
    .product-card {
      border: none;
      border-radius: 16px;
      overflow: hidden;
      transition: transform .18s ease, box-shadow .18s ease;
      background: #fff;
    }
    .product-card:hover { transform: translateY(-4px); box-shadow: 0 12px 28px rgba(0,0,0,.12); }
    .product-thumb { aspect-ratio: 4/3; object-fit: cover; width: 100%; }
    .product-body { padding: 16px 16px 18px 16px; }
    .product-price { color: #198754; font-weight: 700; font-size: 1.1rem; }
    .badge-year { background:#0d6efd10; color:#0d6efd; border:1px solid #0d6efd22; }

    /* CTA */
    .btn-cta {
      padding:.75rem 1.25rem;
      border-radius: 999px;
      font-weight: 600;
    }

    @media (max-width: 575px) {
      .hero { min-height: 56vh; }
      .hero h1 { font-size: 1.9rem; }
    }
  </style>
</head>
<body>

  <%- include('nav') %>

  <!-- HERO -->
  <section class="hero">
    <div class="hero-inner container px-3">
      <span class="badge bg-light text-dark mb-3">Colección Retro</span>
      <h1 class="display-5 mb-2">Revive la historia del fútbol con camisetas legendarias</h1>
      <p class="lead mb-4">Ediciones icónicas de selecciones y clubes. Calidad premium, envíos internacionales.</p>
      <div class="d-flex justify-content-center gap-3">
        <a href="/shirts" class="btn btn-primary btn-cta">
          <i class="bi bi-bag me-1"></i> Ver colección
        </a>
        <a href="#destacados" class="btn btn-outline-light btn-cta">
          <i class="bi bi-stars me-1"></i> Destacados
        </a>
      </div>
    </div>
  </section>

  <!-- DESTACADOS -->
  <main class="container my-5" id="destacados">
    <div class="d-flex align-items-end justify-content-between mb-3">
      <h2 class="section-title m-0">Destacados de la semana</h2>
      <a href="/shirts" class="text-decoration-none">Ver todo <i class="bi bi-arrow-right-short"></i></a>
    </div>

    <% 
      // muestra hasta 8 productos (o menos si no hay)
      const featured = (shirts || []).slice(0, 8);
    %>

    <div class="row g-4">
      <% if (featured.length === 0) { %>
        <div class="col-12">
          <div class="alert alert-light border">No hay productos cargados aún. Agrega camisetas desde el panel de administración.</div>
        </div>
      <% } %>

      <% featured.forEach(function(shirt) { %>
        <div class="col-12 col-sm-6 col-lg-3">
          <div class="product-card h-100">
            <img src="<%= shirt.image %>" alt="Camiseta <%= shirt.team %> <%= shirt.year %>" class="product-thumb">
            <div class="product-body d-flex flex-column">
              <div class="d-flex justify-content-between align-items-center mb-1">
                <span class="badge badge-year"><%= shirt.year %></span>
                <span class="product-price">$<%= shirt.price %></span>
              </div>
              <h6 class="mb-2"><%= shirt.team %></h6>
              <p class="text-muted small flex-grow-1"><%= (shirt.description || '').slice(0, 90) %></p>
              <a href="/shirts/<%= shirt.id %>" class="btn btn-sm btn-outline-primary mt-2">
                Ver detalles
              </a>
            </div>
          </div>
        </div>
      <% }); %>
    </div>
  </main>

  <!-- BENEFICIOS -->
  <section class="container my-5">
    <div class="row g-4">
      <div class="col-12 col-md-4">
        <div class="p-4 bg-white rounded-4 shadow-sm h-100">
          <div class="d-flex align-items-center mb-2"><i class="bi bi-truck fs-3 me-2 text-primary"></i><h6 class="m-0">Envíos internacionales</h6></div>
          <p class="text-muted small m-0">Llegamos a más de 50 países con partners logísticos confiables.</p>
        </div>
      </div>
      <div class="col-12 col-md-4">
        <div class="p-4 bg-white rounded-4 shadow-sm h-100">
          <div class="d-flex align-items-center mb-2"><i class="bi bi-shield-check fs-3 me-2 text-primary"></i><h6 class="m-0">Calidad verificada</h6></div>
          <p class="text-muted small m-0">Materiales premium y controles de calidad en cada prenda.</p>
        </div>
      </div>
      <div class="col-12 col-md-4">
        <div class="p-4 bg-white rounded-4 shadow-sm h-100">
          <div class="d-flex align-items-center mb-2"><i class="bi bi-credit-card fs-3 me-2 text-primary"></i><h6 class="m-0">Pagos seguros</h6></div>
          <p class="text-muted small m-0">Procesamiento cifrado y múltiples métodos de pago.</p>
        </div>
      </div>
    </div>
  </section>

  <%- include('footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

