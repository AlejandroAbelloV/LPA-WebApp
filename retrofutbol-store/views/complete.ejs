<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Compra completada — Retro Futbol Store</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet"/>
  <link rel="stylesheet" href="/stylesheets/style.css"/>

  <style>
    .subhero{background:linear-gradient(180deg,rgba(25,135,84,.08),rgba(25,135,84,0));padding:18px 0;border-bottom:1px solid rgba(0,0,0,.06);}
    .ok-card{border:1px solid rgba(0,0,0,.06);border-radius:16px;background:#fff;}
  </style>
</head>
<body>
  <%- include('nav') %>

  <section class="subhero">
    <div class="container">
      <h1 class="h4 m-0">¡Gracias por tu compra!</h1>
      <p class="text-muted m-0">Tu pedido ha sido procesado correctamente.</p>
    </div>
  </section>

  <main class="container my-4">
    <div class="ok-card p-4 text-center">
      <% 
        const inv = typeof invoice !== 'undefined' && invoice ? invoice 
                   : (typeof lastInvoice !== 'undefined' && lastInvoice ? lastInvoice
                   : (session && session.lastInvoice ? session.lastInvoice : null));
      %>

      <i class="bi bi-check-circle-fill text-success fs-1 mb-2"></i>
      <h5 class="mb-2">¡Pago confirmado!</h5>

      <% if (inv) { %>
        <p class="text-muted mb-1">Número de comprobante</p>
        <h4 class="mb-3"><%= inv.invNo || inv.lpa_inv_no || 'INV—N/A' %></h4>
        <p class="lead">Total pagado: <strong>$<%= inv.amount || inv.lpa_inv_amount || '0' %></strong></p>
      <% } else { %>
        <p class="text-muted">No hay datos de la compra en sesión.</p>
      <% } %>

      <div class="d-flex justify-content-center gap-2 mt-3">
        <a href="/shirts" class="btn btn-outline-primary"><i class="bi bi-bag"></i> Seguir comprando</a>
        <a href="/" class="btn btn-secondary"><i class="bi bi-house"></i> Volver al inicio</a>
      </div>
    </div>
  </main>

  <%- include('footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
